using UnityEngine; 

namespace GameUI
{
    // Base class for keyboard controllers of all robots
    public class KeyboardController : MonoBehaviour
    {
        // empty for now, just used to grab all for the gui.
    }
}